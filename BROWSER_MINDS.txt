FREE + NATIVE + SECURE MINDS (same as Luna Camp 2D)
=====================================================

v61 wires Relics dual talk to the SAME free_minds path as 2D camp.

Order for Talk (Relic duel) / grab chat bubbles:

  1. Free minds — telephanti.com camp API
       POST /api/firmament/camp/banter   (thor ↔ caduceus)
       POST /api/firmament/agent/chat
       • No visitor API keys
       • CORS open, credentials omit
       • Grok when camp has it; aether offline templates otherwise
       • Pill: "Free minds"

  2. Optional telephantim API — local server.py or Render telephantim-ai
       Ollama / Groq / Grok when present
       • Pill: "Relics awake"

  3. Browser-native closed mind — free software in the visitor's browser
       a) Chrome/Edge Built-in AI (Prompt API) when available
       b) WebLLM (MLC AI) — open source WebGPU LLM
          https://webllm.mlc.ai
       • Pill: "Local mind" / "Waking mind…"

  4. Scripted dual duel — always works offline
       • Pill: "Relics ready"

Secure by design
----------------
- Visitors never paste keys into the page
- Free minds call only public camp endpoints (no auth cookies)
- Browser WebLLM runs fully client-side after one model cache
- Characters never mention Ollama / servers / tokens in speech

Code
----
  free-minds.js     — camp bridge (thor→Mjolnir, caduceus→Caduceus)
  native-brain.js   — Chrome AI + WebLLM
  personality.js    — priority chain above

Local PC (strongest private mind)
---------------------------------
  OPEN_RELICS_3D_BRAINS.bat  →  http://127.0.0.1:8765/
  Uses llama3.2 + hermes3 on your machine (no token bill).

UI (v61 cleanup)
----------------
  Float chips: Talk · Reset only (Strike / Hide tray removed)
  Music stays on global Play music chip (all worlds)
  Sheet: Pay · Socials (no duplicate Play music row)
