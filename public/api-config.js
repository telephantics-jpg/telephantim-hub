// Public dual-brain API on Render (works for any visitor — no home PC server)
// Service: telephantim-ai  →  set GROQ_API_KEY or XAI_API_KEY in Render dashboard
window.TELEPHANTIM_API = "https://telephantim-ai.onrender.com";

// Google Analytics 4 — Measurement ID (Admin → Data streams → Web → Measurement ID)
// Same property as Luna Camp by default; create a stream for telephantim.com or paste a new G-XXXX.
// Set to "" to disable tracking.
window.TELEPHANTIM_GA_ID = "G-418J7T1HZ1";
